import React from "react";

const Footer = (props) => {
  return (
    <div className="container p-3 bg-secondary text-light mb-3 text-center">
      Utilisation de React pour lire l'API de Disney en filtrant les films avec
      la liste des personnages.
    </div>
  );
};

export default Footer;
